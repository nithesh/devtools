# pi/prelude backlog (non-spec ideas)

Only items not already captured in `docs/pi/prelude/spec.md` and feature specs.

## Optional prompts/skills (only if repeated need emerges)

- Add minimal prompt templates only after identifying high-frequency repeated prompts.
- Add skills only when long workflows need stronger structure than tools+mode currently provide.

## ask_user UX extras

- Single-question compact UI variant.
- Optional required-answers mode toggle.

## Guardrails future

- Review ecosystem policy/matcher tools before extending custom matcher.
- Optional project severity profiles (strict/dev).

## Web tools future

- Optional deep-fetch backend (`web_fetch_deep`) behind explicit config/capability gate.

## Status/UI extras

- “Calm cockpit” segmented status chips.
- Compact/narrow-width rendering mode.

## Todo extras

- Optional `/todos` interactive viewer.
- Optional priority/tag fields.
